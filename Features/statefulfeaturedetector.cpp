#include "statefulfeaturedetector.h"
#include <iostream>

using namespace std;

void StatefulFeatureDetector::detect(cv::InputArray image, std::vector<cv::KeyPoint>& keypoints, cv::InputArray mask)
{
    cerr << "StatefulFeatureDetector::detect" << endl;
}
